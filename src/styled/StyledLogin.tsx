import styled from 'styled-components';

const LoginInputs = styled.div`
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 1rem;
`;

const LoginButtons = styled.div`
    display: flex;
    gap: 0.5rem;
`;

export { LoginInputs, LoginButtons };
